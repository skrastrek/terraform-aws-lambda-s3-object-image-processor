{
  "name": "lambda-s3-object-image-processor",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "dependencies": {
    "axios": "^1.20.0"
  },
  "devDependencies": {
    "@aws-sdk/client-s3": "^3.1121.0",
    "@types/aws-lambda": "^8.10.162",
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.2",
    "sharp": "^0.35.3"
  },
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --platform=node --target=node24 --external:sharp --outdir=dist && cd sharp-bundle && npm ci --os=linux --cpu=arm64 sharp && cp -r node_modules ../dist/node_modules"
  }
}
